#include<iostream>
#include<string>
using namespace std;
void main()
{
    char a[30];
    int x, b[10] = { 0 };
    cin >> a;
    cout << endl;
    for (int i=0; a[i] != '\0';i++)
    if (a[i] >= '0'&& a[i] <= '9')
    {
        x = a[i] - 48;
        b[x]++;
    }
    for (int i = 0; i <= 9; i++)
        cout << i <<"    "<< b[i] << 
